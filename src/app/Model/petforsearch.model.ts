export class PetSearching {
    Id: number;
    Name: string;
    PhoneNumber: string;
    constructor(id: number, name: string, ph: string) {
      this.Id = id;
      this.Name = name;
      this.PhoneNumber = ph;
    }
  }
  